# Rust ASCII image printer

### Description:
- `--image` - path to the image
- `--w` - width of the output image
- `--h` - height of the output image


### Example:
`ascii-image-printer --image ./image.jpg --w 100 --h 100`




